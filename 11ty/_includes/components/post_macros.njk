{## 
# macro to create header from a given title and tag list.
#}
{% macro header(title, tags) %}
  {# blog title #}
  <h1 class="post-title">{{ title }}</h1>
  <div class="info">{{page.date | formatDate}}</div>
  {# tags #}
  <div class="tags">
    {% for tag in tags %}
      <a class="tag" href= "{{ ('tags/' + tag) | url }}">
        {{ tag }}
      </a>
    {% endfor %}
  </div>
{% endmacro %}

{## 
# create a <ul> list of posts that belong to the
# `collection`, and the title of which doesn't match `title` 
#  `root` being the relative path of the root w.r.t the caller.
#}
{% macro postList(collection) %}
  <div class = "post-list">
    <ul>
      {% for post in collection %}
        <li>
          <a href="{{ post.url | url }}">{{ post.data.title }}
          </a>
          <span class="post-date"> ({{ post.date | formatDate }}) </span>
        </li>
        
      {% endfor %}
    </ul>
  </div>
{% endmacro %}